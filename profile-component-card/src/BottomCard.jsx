export default function BottomCard ({className , title, placeholder}){

    return (
        <div className="bottom_group_card">
            <h1>{title}</h1>
            <p>{placeholder}</p>
        </div>
    )

}